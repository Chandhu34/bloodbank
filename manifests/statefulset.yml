apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
spec:
  serviceName: "mysql"
  replicas: 1
  selector:
    matchLabels:
      app: app-database
  template:
    metadata:
      labels:
        app: app-database
    spec:
      containers:
      - name: nginx
        image: chandu34/bloodbank:dbimage
        ports:
        - containerPort: 80
          name: web
        volumeMounts:
        - name: htt
          mountPath: /usr/share/nginx/html
      imagePullSecrets:
            - name: dockerhub-secret
  volumeClaimTemplates:
  - metadata:
      name: htt
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
